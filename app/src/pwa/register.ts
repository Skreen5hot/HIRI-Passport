import { assetUrl } from "../config/base-path";
export type PwaRegistrationState = "unsupported" | "registered" | "update-ready" | "failed";
export async function registerPassportServiceWorker(notify: (state: PwaRegistrationState, registration?: ServiceWorkerRegistration) => void = () => {}) { if (!("serviceWorker" in navigator)) { notify("unsupported"); return; } try { const registration = await navigator.serviceWorker.register(assetUrl("service-worker.js"), { scope: assetUrl(""), type: "module" }); notify(registration.waiting ? "update-ready" : "registered", registration); registration.addEventListener("updatefound", () => registration.installing?.addEventListener("statechange", () => { if (registration.waiting) notify("update-ready", registration); })); } catch { notify("failed"); } }
