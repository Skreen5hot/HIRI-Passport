export function UpdatePrompt({ registration, busy = false }: { registration: ServiceWorkerRegistration | null; busy?: boolean }) { if (!registration?.waiting) return null; return <aside className="lifecycle-card" role="status"><span>{busy ? "Update waits until this sensitive action finishes." : "A reviewed application update is ready."}</span><button disabled={busy} onClick={() => { registration.waiting?.postMessage("HIRI_SKIP_WAITING"); window.location.reload(); }}>Reload to update</button></aside>; }
