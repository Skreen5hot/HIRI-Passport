import { useState } from "react";
export function VerifierIngressRoute() { const [enabled, setEnabled] = useState(false); return <section className="stack"><header><p className="eyebrow">Later release workspace</p><h1>Verify without flattening the evidence.</h1><p className="lede">The verifier keeps request, holder, credential, identity, status, BVP evidence, and policy results separate.</p></header><section className="panel"><h2>{enabled ? "Synthetic verifier enabled" : "Holder-first release"}</h2><p className="muted">This workspace cannot read the holder portfolio and does not upload claim content.</p><button className="button secondary" onClick={() => setEnabled(true)} disabled={enabled}>{enabled ? "Enabled" : "Enable synthetic workspace"}</button></section></section>; }
