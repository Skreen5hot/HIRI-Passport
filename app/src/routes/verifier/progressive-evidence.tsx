import { EvidenceDimension } from "../../components/evidence/evidence-dimension";
import type { EvidenceState, PolicyState } from "../../types";
export function ProgressiveEvidence({ report }: { report: Record<string, unknown> }) { const disposition = (report.cryptographicDisposition ?? "unknown") as EvidenceState; const policy = ((report.policy as { result?: string } | undefined)?.result ?? "not-evaluated") as PolicyState; return <div className="evidence-grid"><EvidenceDimension label="Request" value={(report.request as { result?: EvidenceState } | undefined)?.result ?? "unknown"} /><EvidenceDimension label="Holder" value={(report.holder as { result?: EvidenceState } | undefined)?.result ?? "unknown"} /><EvidenceDimension label="Cryptography" value={disposition} /><EvidenceDimension label="Current status" value="unknown" /><EvidenceDimension label="Policy" value={policy === "accepted" ? "valid" : policy === "rejected" ? "invalid" : "unknown"} /></div>; }
