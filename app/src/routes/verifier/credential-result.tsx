export function CredentialResult({ credential }: { credential: Record<string, unknown> }) { return <article className="panel"><p className="eyebrow">Credential phase</p><h2>{String(credential.presentationItemId ?? "Presented credential")}</h2><p className="status-line"><span className={`status-dot ${String(credential.result)}`} />Cryptography: {String(credential.result ?? "unknown")}</p><p className="muted">Status: {String(credential.status ?? "unknown")} · provenance: {String(credential.provenance ?? "unknown")}</p></article>; }
