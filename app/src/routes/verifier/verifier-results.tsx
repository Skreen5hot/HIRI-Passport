import { ProgressiveEvidence } from "./progressive-evidence";
import { CredentialResult } from "./credential-result";
export function VerifierResults({ report }: { report: Record<string, unknown> }) { const credentials = (report.credentials as Array<Record<string, unknown>> | undefined) ?? []; return <section className="stack"><header><p className="eyebrow">Progressive verification</p><h1>Separate facts, retained.</h1><p className="lede">Policy cannot recolor invalid cryptography, and status remains unknown without fresh issuer-authoritative evidence.</p></header><ProgressiveEvidence report={report} />{credentials.map((credential, index) => <CredentialResult key={String(credential.presentationItemId ?? index)} credential={credential} />)}<a className="button secondary" href="#/verify/export">Export report</a></section>; }
