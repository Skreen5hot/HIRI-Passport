import type { CryptoCapabilities } from "../adapters/crypto-capabilities";
export function UnsupportedBrowserRoute({ capabilities }: { capabilities?: CryptoCapabilities }) { const missing = capabilities ? Object.entries(capabilities).filter(([name, available]) => name !== "protocolReady" && !available).map(([name]) => name) : []; return <section className="panel stack"><p className="eyebrow">Capability check</p><h1>This browser cannot create a Passport authority.</h1><p className="lede">A required cryptographic or durable-storage operation is unavailable. HIRI does not silently substitute algorithms.</p>{missing.length > 0 && <p><b>Unavailable:</b> {missing.join(", ")}</p>}<p className="muted">You may inspect the clearly marked synthetic demo, but it is not protocol-ready.</p></section>; }
