import { useState } from "react";
import { useAppState } from "../../state/app-state";
export function AuthoritySetupRoute() { const { setAuthorityReady } = useAppState(); const [created, setCreated] = useState(false); const create = () => { setAuthorityReady(true); setCreated(true); }; return <section className="panel stack"><p className="eyebrow">Authority setup</p><h1>{created ? "Authority ready." : "Create your local authority."}</h1><p className="lede">The production flow generates Ed25519 and X25519 keys through capability-verified browser cryptography. This project-origin build creates synthetic demo state only.</p>{created ? <div className="actions"><a className="button" href="#/onboarding/backup">Set up backup</a><a className="button secondary" href="#/home">Continue without backup</a></div> : <button className="button" onClick={create}>Create synthetic authority</button>}</section>; }
