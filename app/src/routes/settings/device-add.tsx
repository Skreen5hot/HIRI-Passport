export function DeviceAddRoute() { return <section className="panel stack"><p className="eyebrow">Add recipient</p><h1>Authorize another device.</h1><p>Use fresh Mode 2 encryption state. A recipient count is not an exact device inventory.</p><div className="field"><label htmlFor="device-label">Local device label</label><input id="device-label" autoComplete="off" /></div><button className="button">Create one-time transfer</button></section>; }
