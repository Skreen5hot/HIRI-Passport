import type { CredentialRecord } from "../../types";
import { SafeText } from "../../security/safe-text";
export function RecordCard({ record }: { record: CredentialRecord }) { const state = record.status === "active" ? "valid" : record.status === "revoked" ? "invalid" : ""; return <article className="panel record-card"><div className="record-top"><span className={`status-dot ${state}`} aria-hidden="true" /><span>{record.status}</span><span className="record-provenance">{record.provenance}</span></div><h2><SafeText>{record.title}</SafeText></h2><p className="muted"><SafeText>{record.issuer}</SafeText></p><p className="small">{record.publicContent ? "Complete public claim" : "Private local assertion"}</p><a className="text-link" href={`#/credential?id=${encodeURIComponent(record.recordId)}`}>Inspect evidence <span aria-hidden="true">→</span></a></article>; }
