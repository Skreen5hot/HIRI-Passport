import { useState } from "react";
import { parseStrictJson } from "../../../../src/sdk/strict-json.mjs";
export function AcquisitionReview({ text, onBack }: { text: string; onBack: () => void }) { const [added, setAdded] = useState(false); let message = "Structure parsed. Cryptographic evidence requires the browser adapter milestone."; try { parseStrictJson(text); } catch (error) { message = `Import blocked: ${(error as Error).message}`; } return <section className="panel stack"><p className="eyebrow">Review before adding</p><h2>{added ? "Added to the synthetic portfolio" : "Import evidence"}</h2><p className="muted">{message}</p><div className="actions">{!added && <button className="button" onClick={() => setAdded(true)}>Add synthetic record</button>}<button className="button secondary" onClick={onBack}>Back</button></div></section>; }
