import { useState } from "react";
import { AcquisitionReview } from "./acquisition-review";
export function AcquireRoute() { const [text, setText] = useState(""); const [review, setReview] = useState(false); return <section className="stack"><header><p className="eyebrow">Credential acquisition</p><h1>Add evidence, not a profile.</h1><p className="lede">Import a complete credential package. Nothing is added until its structure and available evidence have been reviewed.</p></header>{review ? <AcquisitionReview text={text} onBack={() => setReview(false)} /> : <section className="panel"><div className="field"><label htmlFor="credential-json">Paste credential or package JSON</label><textarea id="credential-json" value={text} onChange={event => setText(event.target.value)} placeholder="{ … }" /></div><div className="actions"><button className="button" disabled={!text.trim()} onClick={() => setReview(true)}>Review import</button><button className="button secondary" type="button">Choose file</button></div></section>}</section>; }
