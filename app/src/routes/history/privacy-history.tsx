import { useAppState } from "../../state/app-state";
export function PrivacyHistoryRoute() { const { history } = useAppState(); return <section className="stack"><header><p className="eyebrow">Local privacy history</p><h1>What left this device.</h1><p className="lede">This local log records disclosures and delivery state. It is never published or used for analytics.</p></header>{history.map(event => <article className="panel" key={event.id}><div className="record-top"><span>{event.delivery}</span><span className="record-provenance">{new Date(event.at).toLocaleString()}</span></div><h2>{event.verifier}</h2><p>{event.purpose}</p><a className="text-link" href={`#/history/detail?id=${encodeURIComponent(event.id)}`}>View local receipt</a></article>)}</section>; }
