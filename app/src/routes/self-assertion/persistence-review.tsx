import type { SelfAssertionDraft } from "../../services/self-assertion-service";
export function PersistenceReview({ draft, onBack }: { draft: SelfAssertionDraft; onBack: () => void }) { return <section className="panel stack"><p className="eyebrow">Persistence review</p><h1>{draft.persistence === "ephemeral" ? "Use once." : "Save on this device."}</h1><dl className="claim-list"><div><dt>Label</dt><dd>{draft.label}</dd></div><div><dt>Value</dt><dd>{draft.value}</dd></div><div><dt>Provenance</dt><dd>Self-asserted</dd></div></dl><p className="muted">A verifier must not treat this statement as issuer evidence.</p><div className="actions"><a className="button" href={draft.persistence === "persistent" ? "#/home" : "#/request"}>Confirm</a><button className="button secondary" onClick={onBack}>Back</button></div></section>; }
