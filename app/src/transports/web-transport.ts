export async function postPresentation(destination: string, bytes: Uint8Array, signal?: AbortSignal) { const url = new URL(destination); if (url.protocol !== "https:") throw new TypeError("Presentation delivery requires HTTPS."); const response = await fetch(url, { method: "POST", headers: { "content-type": "application/json" }, body: new Uint8Array(bytes), signal, cache: "no-store", credentials: "omit", redirect: "error" }); if (!response.ok) throw new Error("The destination did not acknowledge delivery."); return { delivered: true, status: response.status }; }
