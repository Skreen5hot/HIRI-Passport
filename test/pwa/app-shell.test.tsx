import { render, screen } from "@testing-library/react"; import { describe, expect, it } from "vitest"; import { AppShell } from "../../app/src/components/app-shell";
describe("app shell", () => { it("provides landmarks, preview limitations, skip navigation, and a synthetic-data warning", () => { render(<AppShell demo><p>Content</p></AppShell>); expect(screen.getByRole("link", { name: "Skip to content" })).toBeVisible(); expect(screen.getByRole("navigation", { name: "Primary navigation" })).toBeVisible(); expect(screen.getByRole("link", { name: /preview limitations and platform support/i })).toHaveAttribute("href", "/preview/"); expect(screen.getByText(/no real keys or credentials/i)).toBeVisible(); }); });
