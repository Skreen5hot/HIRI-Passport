// @vitest-environment node
import { afterEach, describe, expect, it } from "vitest"; import { closePassportDatabase, openPassportDatabase } from "../../app/src/storage/database"; import { STORES } from "../../app/src/storage/schema";
describe("IndexedDB state", () => { afterEach(() => closePassportDatabase()); it("creates every versioned store and migration journal", async () => { const db = await openPassportDatabase(); expect([...db.objectStoreNames]).toEqual(expect.arrayContaining([...STORES])); const journal = await new Promise<unknown>((resolve, reject) => { const request = db.transaction("migrations").objectStore("migrations").get("v1"); request.onsuccess = () => resolve(request.result); request.onerror = () => reject(request.error); }); expect(journal).toMatchObject({ id: "v1" }); }); });
